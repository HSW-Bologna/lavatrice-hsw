#include <stdio.h>
//#include "model/model.h"
//#include "controller/controller.h"


int main(void) {
    //model_t model;

    //model_init(&model);
    // view_init(&model);
    //controller_init(&model);

    printf("Begin main loop\n");
    for (;;) {
    }

    return 0;
}
